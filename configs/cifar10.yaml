# CIFAR-10: 32x32 RGB images → input_dim = 32*32*3 = 3072
data:
  dataset: cifar10
  batch_size: 128

model:
  input_dim: 3072          # 32 * 32 * 3
  latent_dim: 20
  hidden_dim: 400
  arch: fc

training:
  epochs: 30
  lr: 1.0e-3
  optimizer: adam
  beta: 1.0
  seed: 42
